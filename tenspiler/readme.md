Mark a driver with # This driver fails. if it enters the search part but reports synthesis fails.
Mark a driver with # This driver does not handle so many loops. if it has more than two loops to handle.
Mark a driver with # This driver does not work. if it does not enter the search part.